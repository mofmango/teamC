<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mnu.mapper.ReportMapper">

    <insert id="create">
        INSERT INTO TBL_REPORT (report_id, bno, reporter_id, reported_id, report_content)
        VALUES (seq_report.nextval, #{bno}, #{reporter_id}, #{reported_id}, #{report_content})
    </insert>

    <select id="getList" resultType="org.mnu.domain.ReportVO">
        SELECT * FROM TBL_REPORT ORDER BY report_date DESC
    </select>

</mapper>