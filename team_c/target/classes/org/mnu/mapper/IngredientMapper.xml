<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mnu.mapper.IngredientMapper">

    <select id="getListByUser" resultType="string">
        SELECT ingredient_name FROM TBL_USER_INGREDIENTS WHERE userid = #{userid} ORDER BY ingredient_name ASC
    </select>

    <insert id="addIngredient">
        INSERT INTO TBL_USER_INGREDIENTS (userid, ingredient_name) VALUES (#{userid}, #{name})
    </insert>

    <delete id="removeIngredient">
        DELETE FROM TBL_USER_INGREDIENTS WHERE userid = #{userid} AND ingredient_name = #{name}
    </delete>

</mapper>