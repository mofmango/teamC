<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mnu.mapper.CommentMapper">
    <insert id="create">
        INSERT INTO TBL_COMMENT (rno, bno, reply, replyer)
        VALUES (seq_comment.nextval, #{bno}, #{reply}, #{replyer})
    </insert>
    <select id="getListByBno" resultType="org.mnu.domain.CommentVO">
        SELECT * FROM TBL_COMMENT WHERE bno = #{bno} ORDER BY rno ASC
    </select>
    
    <delete id="deleteByBno">
	    DELETE FROM TBL_COMMENT WHERE bno = #{bno}
	</delete>
    
    
</mapper>